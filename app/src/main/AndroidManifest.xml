<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.discoverandchange.pornographycrisissupport">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name="App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".quiz.QuizController">
        </activity>
        <activity android:name=".SupportNetworkList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".library.LibraryController">
        </activity>

        <provider
            android:authorities="com.discoverandchange.pornographycrisissupport.db.scoresprovider"
            android:name=".db.ScoresProvider"
            android:exported="false" />
    </application>

</manifest>